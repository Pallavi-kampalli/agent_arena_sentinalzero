# Agent Arena SupportOps — Participant Configuration

# Local Practice Mode (Mock Simulator)
BASE_URL=http://localhost:8000
BEARER_TOKEN=dev-practice-token

# Live Production Competition Mode:
# Once the competition opens, update these two variables with your organizer-issued credentials.
# No agent code or SDK changes are required.
# BASE_URL=https://arena.competition.org
# BEARER_TOKEN=your-assigned-team-bearer-token
