# Agent Arena SupportOps — Participant Configuration

# Mode 1: Local Mock Simulator (Practice Mode)
BASE_URL=http://127.0.0.1:8001
BEARER_TOKEN=dev-practice-token

# Mode 2: Live Agent Arena Platform (Competition Mode)
# Switch to these variables when connecting to local Docker Compose or hosted competition platform:
# BASE_URL=http://localhost:8000
# BEARER_TOKEN=your-assigned-team-bearer-token
